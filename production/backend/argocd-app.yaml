apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pdm-backend-production
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/yoavmanoim-prog/pdm-app
      targetRevision: HEAD
      path: helm/backend
      helm:
        valueFiles:
          - $values/production/backend/values.yaml
    - repoURL: https://github.com/yoavmanoim-prog/pdm-gitops
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: pdm-production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
